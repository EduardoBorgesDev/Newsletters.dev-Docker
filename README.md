# Comando de inicialização do zero e limpa

# docker-compose down; docker-compose pull; docker-compose build --no-cache; docker-compose up -d

# Buildar rapidamente: docker-compose up --build -d
# LINK YOUTUBE: